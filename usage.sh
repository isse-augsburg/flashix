#!/bin/sh

echo "build with:    ./build.sh"
echo "mount with:    ./run.sh [-odebug] [-obig_writes] <mountpoint>"
echo "test with:     ./test.sh <mountpoint> <path/to/test-suite>"
echo "unmount with:  fusermount -u <mountpoint>"

